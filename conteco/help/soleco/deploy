#
# deploy [arguments] <selector>
# Use the --info flag to get config specific container image information.
#
# This interface manages the deployment of the service stacks (all types) and the repository (module type only).
#
# deploy module [up/downup/down]
# This method deploys, redeploys or removes the module network and volumes.
#
# deploy [stack]] [up/downup/down]
# This method deploys, redeploys or removes the named service stack.
#
